enum PaletteType {
  P2BitIndexed = '2-bit (4 color) Indexed',
  P4BitRGBI = '4-bit (16 color) RGBI',
  P4BitIndexed = '4-bit (16 color) Indexed',
  PMono = 'Monochrome',
  PRGB = 'RGB',
  PAuto = 'Optimized (Auto)',
  POther = 'Other'
}

export default PaletteType;